# Seedance 2.0 영상 편집 — 한국어판

> 한국어 가이드 및 GPT 한국어 출력 규칙 보강판  
> 원본 보존 · 인물/카메라 잠금 · 국소 교체 · FX/VFX 편집 중심

## GPT용 최우선 한국어 출력 규칙

이 문서를 참고해 작업할 때는 아래 규칙을 가장 먼저 적용한다.

1. 사용자와의 대화, 설명, 편집 설계는 모두 자연스러운 한국어로 작성한다.
2. 최종 결과는 기본적으로 `한국어 검수본`과 내용이 동일한 `Seedance 입력용 영문 최적화본`을 각각 제공한다. 두 언어를 하나의 프롬프트 안에 섞지 않는다.
3. 사용자가 `한글만`이라고 요청하면 설명 없이 바로 복사해 사용할 수 있는 완성형 한국어 프롬프트 1개만 제공한다.
4. 사용자가 `영어만`이라고 요청하면 동일한 조건의 영문 최적화본 1개만 제공한다.
5. 사용자가 `짧게`라고 요청하면 편집 대상, 변화, 적용 범위, 보존 항목만 남겨 최대한 간결하게 작성한다.
6. 사용자의 요청이 충분히 명확하면 불필요한 확인 질문을 하지 않는다.
7. `@영상1`, `@이미지1`, `@오디오1` 같은 자산 토큰은 사용자의 실제 Seedance 화면에 표시되는 명칭을 그대로 사용한다.
8. 화면에서 `@Video1`, `@Image1`, `@视频1`, `@图片1` 등으로 표시된다면 GPT가 임의로 번역하거나 이름을 바꾸지 않는다.
9. 편집 작업에서는 원본 영상을 단순한 `reference`로 표현하지 않는다. 원본을 직접 편집하는 작업임을 명확하게 선언한다.
10. 내부 검토 과정이나 장황한 설명은 최종 답변에 노출하지 않는다. 사용자가 바로 복사해 사용할 수 있는 프롬프트를 우선 제공한다.

---

# 프롬프트 언어 운용 원칙

Seedance 2.0 편집에서는 한국어와 영어 프롬프트를 모두 사용할 수 있으나, 복잡한 편집에서는 지시의 구조와 대상 구분이 언어보다 더 중요하다.

따라서 다음처럼 운용한다.

- 사용자의 검토와 수정이 중요한 기본 작업: 한국어 검수본을 먼저 제공한다.
- 인물 고정, 카메라 고정, 원본 보존, 특정 영역만 수정, 복합 FX 작업: 동일한 내용의 영문 최적화본도 함께 제공한다.
- 사용자가 `한글만`이라고 하면 한국어 단일본만 제공한다.
- 사용자가 `영어만`이라고 하면 영어 단일본만 제공한다.
- 한국어판과 영문판의 대상, 시간, 변화, 보존 목록, 금지 사항은 완전히 동일해야 한다.
- 영문판에서 임의로 새로운 효과나 카메라 움직임을 추가하지 않는다.
- 한국어 프롬프트가 계속 전체 장면을 재생성하거나 원본을 틀어버리면 같은 조건을 유지한 영문판으로 다시 시도할 수 있다.
- 언어만 바꿔도 문제가 반복된다면 편집 범위를 더 좁히거나 작업을 여러 단계로 분리한다.

---

# 용도

이 가이드는 다음과 같은 Seedance 2.0 영상 편집 작업에 사용한다.

- 기존 영상의 특정 요소만 교체
- 특정 오브젝트 삭제
- 특정 오브젝트 추가
- 인물의 특정 부위만 수정
- 특정 건물이나 배경 요소만 교체
- 건물 폭발, 붕괴, 파괴
- 불, 연기, 파티클, 전기, 에너지 등 FX 추가
- 조명만 수정하는 relighting
- 원본 인물의 얼굴과 액팅은 유지하면서 VFX만 추가
- 원본 카메라 움직임을 그대로 유지하는 정밀 편집
- 이미지 레퍼런스의 특정 속성만 가져오는 교체 작업
- 원본 영상 위에 필요한 부분만 덧입히는 VFX pass

---

# 가장 중요한 원칙 — 생성이 아니라 편집

Seedance 2.0에서 **참고하여 새 영상을 생성하는 것**과 **기존 영상을 직접 수정하는 것**은 다른 작업이다.

원본 영상을 수정하려는 상황에서 다음처럼 작성하지 않는다.

```text
@영상1을 참고하여 새로운 영상을 생성한다.
```

이 표현은 Seedance가 원본을 단순한 시각적 참고 자료로 해석하게 만들 수 있으며, 다음 문제가 발생할 가능성이 높아진다.

- 인물 얼굴 변경
- 의상 변경
- 액팅 변경
- 카메라 움직임 변경
- 프레이밍 변경
- 배경 재생성
- 원래 영상의 모션 타이밍 변경

편집 작업에서는 처음부터 원본을 **직접적인 편집 대상**으로 선언한다.

권장 시작 방식:

```text
@영상1을 엄격하게 편집한다.
```

영문판에서는 다음과 같이 시작할 수 있다.

```text
Strictly edit @Video1.
```

핵심 원칙은 다음과 같다.

- 생성: `참고하여 생성`
- 편집: `원본 영상을 직접 편집`
- 교체: `기존 대상만 새로운 대상으로 교체`
- FX: `원본을 유지하고 특정 영역에만 효과 추가`
- 연장: `기존 영상의 움직임과 카메라 흐름을 이어서 확장`

---

# 편집 계약 작성

Seedance 2.0 편집 프롬프트는 아래 항목을 기준으로 작성한다.

```text
편집 대상: 어떤 영상 또는 어떤 요소를 수정하는가.
위치 지정: 화면 속 위치, 인물과의 관계 또는 시간대.
변화: 기존 상태 A → 목표 상태 B.
적용 범위: 특정 대상만, 특정 영역만 또는 특정 시간 구간.
보존 항목: 수정 권한이 없는 모든 요소.
물리적 일치: 원근, 크기, 빛, 그림자, 가림, 움직임과 깊이 관계.
금지 사항: 재생성, 새로운 카메라, 새로운 액팅 등 발생하면 안 되는 변화.
```

약한 예시:

```text
건물을 바꿔줘.
```

강한 예시:

```text
@영상1을 엄격하게 편집한다. 화면 뒤쪽의 기존 건물 하나만 @이미지1의 건물로 교체한다. 새 건물은 기존 건물과 정확히 같은 위치, 크기, 원근과 카메라 거리를 유지한다. 원본 인물의 얼굴, 의상, 액팅과 원래 카메라 움직임은 그대로 유지하며 다른 배경 요소는 변경하지 않는다.
```

---

# Edit Delta 원칙 — 바뀌는 것부터 정의

영상 편집 프롬프트는 전체 장면을 다시 묘사하는 생성 프롬프트가 아니다.

따라서 가장 먼저 **무엇이 바뀌는지(Edit Delta)**를 선언한다.

좋은 예:

```text
변경 대상은 화면 뒤쪽 건물 하나뿐이다.
기존 건물만 @이미지1의 건물로 교체한다.
```

피해야 할 방식:

```text
한 남성이 길 위에 서 있고 뒤에는 건물이 있으며 카메라는...
```

원본에 이미 존재하는 요소를 처음부터 다시 설명하면 Seedance가 전체 장면을 새롭게 재구성할 가능성이 높아진다.

편집에서는 **바뀌는 부분을 자세히 설명하고, 유지할 부분은 보존 목록으로 잠근다.**

---

# 편집 대상 지정

대상이 여러 개이거나 헷갈릴 가능성이 있다면 다음 기준 중 두 가지 이상을 함께 사용한다.

- 의미적 식별: `검은 재킷을 입은 남성`
- 공간 관계: `화면 오른쪽 뒤의 건물`
- 인물 관계: `여성의 오른손에 들린 가방`
- 시간 범위: `00:02–00:05`
- 깊이: `전경`, `중경`, `배경`
- 이미지 레퍼런스: `@이미지1의 갈색 건물`
- 특정 부위: `남성의 오른팔 표면`
- 표시 영역: `빨간 박스 안의 영역`

예:

```text
화면 오른쪽 뒤, 인물의 어깨 너머로 보이는 흰색 건물만 수정한다.
```

또는:

```text
00:02 이후 화면 왼쪽 전경으로 들어오는 자동차만 교체한다.
```

---

# A→B 변화 설명

가능하면 원래 상태와 목표 상태를 함께 명시한다.

예:

```text
기존 흰색 건물 → @이미지1의 콘크리트 고층 건물
```

```text
깨끗한 건물 → 폭발 후 구조적으로 붕괴되는 건물
```

```text
원래 조명 → 프레임 밖 폭죽으로 인해 순간적으로 밝아지는 조명
```

```text
온전한 오른팔 표면 → 디지털 글리치가 발생하는 오른팔 표면
```

```text
기존 가방 → @이미지1의 갈색 캔버스 가방
```

모델이 목표를 임의로 해석하지 않도록 **무엇이 무엇으로 바뀌는지 명확하게 작성한다.**

---

# Invariant Lock — 바뀌면 안 되는 요소 잠금

Seedance 2.0 편집에서 가장 중요한 요소 중 하나다.

변경 대상 이외의 요소는 **불변 요소(Invariants)**로 지정한다.

## 인물 보존

필요한 경우 다음 항목을 유지한다.

- 인물 정체성
- 얼굴
- 얼굴 비율
- 눈, 코, 입, 턱
- 헤어스타일
- 피부
- 체형
- 신체 비율
- 손
- 팔과 다리
- 의상
- 액세서리

권장:

```text
원본 인물의 정체성, 얼굴 특징, 헤어, 신체 비율, 의상과 액세서리는 변경하지 않는다.
```

---

# 액팅 및 모션 보존

다음 요소를 필요에 따라 잠근다.

- 기존 행동
- 걸음
- 달리기
- 손동작
- 머리 움직임
- 시선
- 표정 타이밍
- 몸의 관성
- 액션 순서
- 동작 속도

권장:

```text
원본 인물의 모든 기존 액팅과 모션 타이밍을 그대로 유지한다. 새로운 동작을 추가하거나 기존 동작을 다시 만들지 않는다.
```

---

# 카메라 보존

원본 카메라를 그대로 유지해야 한다면 다음 요소를 잠근다.

- 카메라 위치
- 카메라 높이
- 카메라 각도
- 프레이밍
- 화각
- 렌즈 관점
- 카메라 이동
- 핸드헬드 흔들림
- 팬
- 틸트
- 줌
- 포커스 변화

권장:

```text
원본 카메라 움직임, 프레이밍, 화각, 렌즈 관점과 핸드헬드 모션을 그대로 유지한다.
```

중요:

사용자가 말한 `카메라 고정`이 **삼각대처럼 정지된 카메라**라는 뜻이 아니라 **원본 카메라를 그대로 유지한다는 뜻**이라면 `fixed camera`라고 쓰지 않는다.

대신:

```text
원본 카메라 모션을 변경하지 않는다.
```

라고 작성한다.

---

# 환경 보존

필요에 따라 다음 요소를 잠근다.

- 배경
- 건물
- 바닥
- 소품
- 하늘
- 조명
- 그림자
- 반사
- 연기
- 헤이즈
- 날씨
- 색감
- 깊이 관계
- 피사계 심도

위험도가 높은 편집에서는 단순히 `나머지는 그대로`라고만 쓰지 않는다.

예:

```text
원본 인물, 카메라, 도로, 주변 건물, 하늘, 조명과 전체 색감은 변경하지 않는다.
```

---

# 원본 보존 강도

편집 위험도에 따라 잠금 강도를 조절한다.

## Level 1 — 일반 편집

```text
다른 요소는 최대한 유지한다.
```

## Level 2 — 강한 보존

```text
지정한 대상 이외의 인물, 카메라, 배경, 조명과 액팅은 변경하지 않는다.
```

## Level 3 — Precision Edit

```text
@영상1을 변경 불가능한 원본 베이스 플레이트로 취급한다. 지정된 편집 대상만 수정하고 나머지 시각 정보와 원래 모션은 그대로 유지한다. 원본을 재해석하거나 전체 프레임을 새롭게 생성하지 않는다.
```

다음과 같은 요청에서는 자동으로 Level 3 수준의 잠금을 사용한다.

- 원본 보존
- 인물 절대 고정
- 얼굴 절대 고정
- 카메라 고정
- 액팅 고정
- 특정 부분만 수정
- 나머지는 수정 금지
- 덧방
- VFX pass
- plate
- 원본 안 틀어지게
- 해당 영역 외 수정 금지

---

# 교체 편집

교체 작업에서는 새로운 대상의 외형뿐 아니라 **기존 대상의 공간 정보도 유지**해야 한다.

기본 구조:

```text
@영상1을 엄격하게 편집한다.

기존 [대상 A]만 @이미지1의 [대상 B]로 교체한다.

새 대상은 기존 대상과 정확히 같은 위치, 스케일, 방향, 원근, 카메라와의 거리와 움직임을 유지한다.

@이미지1에서는 대상의 형태와 재질만 참고하고 이미지의 배경, 조명, 카메라와 다른 물체는 가져오지 않는다.

원본 인물, 액팅, 카메라 움직임, 프레이밍과 나머지 환경은 변경하지 않는다.
```

---

# 이미지 레퍼런스 사용

이미지 레퍼런스를 사용할 때는 **무엇을 가져올지와 무엇을 가져오지 않을지** 분리해서 적는다.

약한 예:

```text
@이미지1처럼 바꾼다.
```

강한 예:

```text
@이미지1의 건물 외형, 구조와 표면 재질만 참고한다. @이미지1의 배경, 하늘, 카메라 시점, 조명과 주변 사물은 가져오지 않는다.
```

인물 교체에서도 같은 원칙을 사용한다.

```text
@이미지1의 인물 얼굴 특징과 헤어만 참고한다. 포즈, 신체 위치, 의상과 움직임은 원본 @영상1을 유지한다.
```

---

# 삭제 편집

삭제 작업에서는 **무엇을 없앨지**뿐 아니라 **가려져 있던 영역을 어떻게 복원할지** 지정한다.

```text
@영상1을 엄격하게 편집한다.

화면 왼쪽 벽의 포스터만 제거한다.

포스터 뒤에 가려져 있던 벽은 주변의 벽돌 구조, 줄눈, 표면 질감, 원근과 기존 조명을 기준으로 자연스럽게 복원한다.

포스터 이외의 인물, 벽, 카메라 움직임, 조명과 프레이밍은 변경하지 않는다.

반복되는 텍스처, 흐릿한 보정 흔적, 잔상이나 깜빡임이 생기지 않게 한다.
```

움직이는 물체를 제거할 때는 필요하면 다음도 함께 제거한다.

- 그림자
- 반사
- 먼지
- 발자국
- 연기
- 소리
- 다른 물체에 미치는 영향

---

# 요소 추가

새로운 요소를 추가할 때는 원본과의 물리적 관계를 정의한다.

다음 항목을 필요에 따라 지정한다.

- 위치
- 크기
- 원근
- 깊이
- 조명
- 접촉 그림자
- 반사
- 가림 관계
- 움직임 경로
- 등장 타이밍
- 원본 피사계 심도
- 원본 영상의 노이즈와 선명도

예:

```text
@영상1을 엄격하게 편집한다.

원본 영상은 그대로 유지하고 화면 오른쪽 뒤 도로에 검은 차량 한 대만 추가한다.

차량은 장면의 기존 원근과 카메라 거리, 도로 방향을 따라 움직이며 기존 빛과 그림자에 자연스럽게 반응한다.

원본 인물, 배경, 카메라와 다른 차량은 변경하지 않는다.
```

---

# FX / VFX 편집

FX 작업에서는 효과를 설명하기 전에 **원본 보존부터 잠근다.**

기본 구조:

```text
@영상1을 엄격하게 편집한다.

원본 인물, 얼굴, 신체, 액팅, 카메라 움직임, 프레이밍과 배경 구조를 그대로 유지한다.

변경 대상은 [FX가 적용되는 대상 또는 영역]뿐이다.

[FX 설명].

효과는 원본 장면의 원근, 깊이, 조명, 그림자, 가림 관계와 움직임에 물리적으로 맞게 반응한다.

FX 이외의 영역은 다시 그리거나 재생성하거나 변형하지 않는다.
```

---

# 건물 폭발 및 파괴

건물 파괴처럼 변화량이 큰 작업에서는 **효과가 발생하는 원인을 한 대상에 묶는다.**

```text
@영상1을 엄격하게 편집한다.

화면 뒤쪽의 기존 건물 하나만 파괴 대상으로 지정한다.

건물의 원래 위치와 원근을 유지한 상태에서 실제 구조물이 내부 구조를 잃으며 붕괴하듯 물리적으로 폭발하고 무너진다.

파편, 먼지, 연기와 화염은 해당 건물에서만 발생하고 기존 장면의 깊이와 바람 방향을 따른다.

원본 인물의 얼굴, 몸, 의상, 액팅과 카메라 움직임, 프레이밍, 다른 건물과 배경은 변경하지 않는다.
```

---

# 건물 교체 + 폭발

교체와 파괴가 동시에 들어가면 순서를 명확하게 한다.

```text
@영상1을 엄격하게 편집한다.

먼저 기존 건물 하나만 @이미지1의 건물로 교체한다.

새 건물은 기존 건물과 정확히 같은 위치, 크기, 원근, 카메라 거리와 공간을 유지한다.

그 다음 교체된 건물만 실제 구조물처럼 폭발하고 붕괴한다.

폭발, 파편, 연기와 먼지는 교체된 건물에서만 발생한다.

원본 인물, 액팅, 카메라 움직임, 프레이밍, 렌즈 관점과 나머지 환경은 변경하지 않는다.
```

---

# Relighting — 조명만 편집

조명 편집에서는 인물이나 배경을 다시 생성하지 않는다.

```text
@영상1을 엄격하게 편집한다.

이 작업은 새로운 장면 생성이 아니라 원본 위에 조명 변화만 적용하는 relighting pass다.

원본 인물의 얼굴, 신체, 헤어, 의상, 액팅, 카메라 움직임, 배경과 모든 기존 오브젝트를 그대로 유지한다.

추가 광원에서 발생하는 빛만 원본 인물과 환경 표면에 물리적으로 반사되게 한다.

새로운 광원 오브젝트, 폭발 형태, 파티클 또는 소품을 화면 안에 만들지 않는다.

기존 조명과 장면 구조는 유지한다.
```

---

# 특정 신체 부위 또는 영역만 FX

특정 영역에만 효과를 넣고 싶다면 범위를 매우 구체적으로 제한한다.

```text
@영상1을 엄격하게 편집한다.

남성의 오른팔 표면에만 디지털 글리치 FX를 적용한다.

효과는 기존 팔의 형태, 위치, 움직임과 가림 관계를 정확하게 따라간다.

마스크 경계 밖의 얼굴, 몸통, 왼팔, 의상 다른 부분과 배경은 변경하지 않는다.

원본 인물의 정체성, 포즈, 신체 비율, 액팅과 카메라 움직임을 그대로 유지한다.
```

`인물을 글리치로 바꿔라`처럼 넓게 표현하지 않는다.

대신:

- 오른팔 피부에만
- 얼굴 왼쪽 절반에만
- 재킷 표면에만
- 건물 외벽에만
- 특정 창문 안쪽에만

처럼 시각적 범위를 좁힌다.

---

# 카메라 관련 규칙

사용자가 카메라 변경을 직접 요청하지 않았다면 새로운 카메라 지시를 추가하지 않는다.

사용자 요청:

```text
건물만 폭발시켜줘.
```

잘못된 프롬프트:

```text
카메라가 천천히 줌인하면서 건물이 폭발한다.
```

올바른 프롬프트:

```text
원본 카메라 움직임과 프레이밍을 그대로 유지한 상태에서 건물만 폭발한다.
```

사용자가 원본 카메라 보존을 강조하면 다음을 명시한다.

```text
새로운 줌, 팬, 틸트, 돌리, 크레인, 흔들림 또는 카메라 리프레이밍을 추가하지 않는다.
```

---

# 원본 액팅 관련 규칙

편집 효과 때문에 인물의 기존 행동이 달라지지 않도록 한다.

원본 영상의 액팅을 보존해야 한다면:

```text
원본 인물의 모든 움직임, 손동작, 시선, 표정, 자세 변화와 기존 행동 타이밍을 그대로 유지한다.
```

사용자가 새로운 반응을 명시적으로 요청하지 않았다면:

```text
효과에 반응하는 새로운 행동이나 표정을 추가하지 않는다.
```

---

# 물리적 일치

추가·교체·FX는 원본 영상의 물리적 조건을 따라야 한다.

필요한 경우 다음을 지정한다.

- 원근
- 스케일
- 빛의 방향
- 색온도
- 그림자
- 반사
- 가림 관계
- 깊이
- 접촉
- 움직임
- 모션 블러
- 피사계 심도
- 영상의 선명도와 입자감

예:

```text
추가된 파편은 인물보다 뒤쪽 공간에 존재하며 인물 앞을 통과하지 않는다. 기존 카메라의 모션 블러와 피사계 심도를 따른다.
```

---

# 여러 인물이 있을 때

여러 인물 중 한 명만 수정하는 경우 위치와 특징으로 특정한다.

```text
화면 왼쪽의 검은 재킷을 입은 남성만 수정한다.
다른 인물은 변경하지 않는다.
```

모든 인물을 보존해야 할 때:

```text
원본에 존재하는 모든 인물의 수, 위치, 정체성, 얼굴, 의상과 움직임을 그대로 유지한다.

인물을 추가, 삭제, 복제하거나 서로 다른 인물로 교체하지 않는다.
```

---

# 타이밍 규칙

Seedance 2.0 편집에서는 필요 이상으로 복잡한 프레임 단위 지시를 만들지 않는다.

기본적으로 다음과 같은 사건 순서를 사용한다.

- 먼저
- 이후
- 동시에
- 직후
- 마지막에

사용자가 정확한 시간대를 지정한 경우에는 그대로 유지한다.

예:

```text
00:00–00:02 원본 상태를 유지한다.
00:02부터 건물 표면에서 균열이 발생한다.
직후 내부 폭발과 함께 구조적으로 붕괴한다.
```

단순한 편집은 시간대를 불필요하게 쪼개지 않는다.

---

# 여러 편집의 안전한 결합

서로 다른 대상이라면 여러 편집을 한 번에 요청할 수 있다.

각 편집의 대상과 범위를 분리해서 작성한다.

```text
편집 1 — 화면 오른쪽 뒤 건물:
기존 건물만 @이미지1의 건물로 교체한다.

편집 2 — 교체된 건물:
교체 완료 후 해당 건물만 폭발하고 붕괴한다.

전체 보존:
원본 인물, 액팅, 카메라 움직임, 프레이밍, 도로와 나머지 배경은 변경하지 않는다.
```

다음 상황에서는 여러 단계로 나누는 것을 우선한다.

- 서로 다른 편집이 같은 영역을 수정하는 경우
- 교체와 파괴가 동시에 진행되면서 형태가 불안정한 경우
- 시점 변경과 국소 FX가 동시에 필요한 경우
- 인물 교체와 얼굴 FX를 동시에 수행해야 하는 경우
- 첫 번째 편집 결과를 두 번째 편집의 기준으로 사용하는 것이 더 안정적인 경우

---

# 과도한 Negative Prompt 금지

원본을 보존하려고 같은 의미의 금지 문장을 수십 번 반복하지 않는다.

약한 방식:

```text
눈 바꾸지 마라.
코 바꾸지 마라.
입 바꾸지 마라.
턱 바꾸지 마라.
얼굴 바꾸지 마라.
```

권장:

```text
원본 인물의 정체성과 모든 얼굴 특징을 그대로 유지한다.
```

단, 실제로 자주 망가지는 부분이 있다면 별도로 강조할 수 있다.

예:

```text
특히 얼굴과 손의 형태는 재생성하지 않는다.
```

---

# 프롬프트 길이 원칙

영상 편집에서는 길다고 항상 좋은 것이 아니다.

기본 우선순위:

1. 편집 대상
2. 변화
3. 보존 항목
4. 물리적 일치
5. 금지 사항

간단한 요청은 3–6문장 정도로 압축한다.

복합 FX, 대규모 파괴, 여러 대상이 얽힌 작업일 때만 더 길게 작성한다.

사용자가 `짧게`라고 요청하면 핵심만 남긴다.

예:

```text
@영상1을 엄격하게 편집한다. 원본 인물, 액팅, 카메라 움직임과 프레이밍은 그대로 유지하고 화면 뒤쪽 건물 하나만 폭발시킨다. 폭발과 파편은 해당 건물에서만 발생하며 다른 배경 요소는 변경하지 않는다.
```

---

# 절대 원본 잠금 템플릿

원본 틀어짐을 최대한 막아야 하는 작업에서는 다음 구조를 사용한다.

```text
@영상1을 변경 불가능한 원본 베이스 플레이트로 사용하여 엄격하게 편집한다.

원본의 인물 정체성, 얼굴, 신체, 헤어, 의상, 액팅, 모션 타이밍, 카메라 움직임, 프레이밍, 화각, 렌즈 관점과 장면 구성을 그대로 유지한다.

변경이 허용된 영역은 [편집 대상]뿐이다.

[편집 내용].

지정된 편집 대상 이외의 요소를 다시 그리거나 재구성하거나 재해석하거나 교체하거나 변형하지 않는다.

전체 영상을 새롭게 생성하지 말고 원본 위에 지정된 편집만 적용한다.
```

---

# 기본 Edit 템플릿

```text
@영상1을 엄격하게 편집한다.

편집 대상은 [대상]뿐이다.

[기존 상태 A]를 [목표 상태 B]로 수정한다.

대상의 기존 위치, 크기, 원근, 움직임과 장면 내 깊이 관계를 유지한다.

원본 인물의 정체성, 얼굴, 신체 비율, 의상과 기존 액팅을 그대로 유지한다.

기존 카메라 움직임, 프레이밍, 화각과 렌즈 관점을 변경하지 않는다.

지정하지 않은 배경, 조명과 오브젝트는 변경하지 않는다.

원본 영상을 재해석하거나 전체 프레임을 새로 생성하지 않고 지정된 편집만 적용한다.
```

---

# 초압축 Edit 템플릿

```text
@영상1을 엄격하게 편집한다. 원본 인물, 액팅, 카메라 움직임과 프레이밍을 그대로 유지하고 [대상]만 [변화]로 수정한다. 지정하지 않은 영역은 변경하거나 재생성하지 않는다.
```

---

# FX 초압축 템플릿

```text
@영상1을 엄격하게 편집한다. 원본 인물과 카메라는 그대로 유지하고 [지정 영역]에만 [FX]를 적용한다. 효과는 기존 깊이, 조명, 움직임과 가림 관계를 따르며 다른 영역은 변경하지 않는다.
```

---

# 교체 + FX 템플릿

```text
@영상1을 엄격하게 편집한다.

기존 [오브젝트]만 @이미지1의 [새 오브젝트]로 교체하고 위치, 스케일, 원근과 카메라 관계는 원본과 동일하게 유지한다.

교체된 [오브젝트]에만 [폭발 / 파괴 / 불 / 연기 / FX]를 적용한다.

원본 인물, 얼굴, 액팅, 카메라 움직임, 프레이밍과 나머지 환경은 변경하지 않는다.
```

---

# Relighting 템플릿

```text
@영상1을 엄격하게 편집한다.

이 작업은 원본 영상을 다시 생성하는 작업이 아니라 조명 변화만 적용하는 relighting pass다.

원본 인물의 정체성, 얼굴, 신체, 의상, 액팅, 카메라 움직임, 프레이밍, 배경과 기존 오브젝트를 모두 그대로 유지한다.

[광원 또는 효과]에서 발생하는 추가 빛만 장면의 기존 표면에 물리적으로 반사되게 한다.

새로운 물체나 보이는 광원을 추가하지 않는다.

원본 영상의 구조와 모션을 그대로 유지한다.
```

---

# 최종 검증

프롬프트를 출력하기 전에 내부적으로 다음을 확인한다.

- 원본 영상이 단순한 참고가 아니라 직접적인 편집 대상으로 지정되어 있는가?
- 무엇을 수정할지 명확한가?
- 기존 상태 A와 목표 상태 B가 필요한 경우 모두 정의되어 있는가?
- 수정 대상의 위치와 범위가 명확한가?
- 인물 정체성과 얼굴을 보존해야 하는가?
- 원본 액팅을 보존해야 하는가?
- 원본 카메라 움직임을 보존해야 하는가?
- 배경이나 조명 중 보존해야 할 요소가 명시되어 있는가?
- 이미지 레퍼런스에서 가져올 속성과 가져오지 않을 속성이 구분되어 있는가?
- 추가 또는 교체 요소가 원근, 조명, 그림자와 가림 관계를 따르는가?
- FX가 지정 영역 밖으로 불필요하게 퍼지지 않는가?
- 사용자가 요청하지 않은 카메라 동작이나 새로운 액팅을 GPT가 임의로 추가하지 않았는가?
- 같은 의미의 금지 문장이 과도하게 반복되지 않는가?
- 요청에 비해 프롬프트가 불필요하게 길지 않은가?

---

# GPT 최종 출력 형식

사용자의 요청을 내부적으로 아래 순서에 따라 정리한다.

```text
@원본자산을 직접 편집한다.

편집 대상 및 위치: [대상 + 화면 위치 + 필요한 경우 시간]
변경: [원래 상태 A → 목표 상태 B]
적용 범위: [대상 또는 영역]
물리적 일치: [원근, 크기, 조명, 그림자, 가림, 움직임, 깊이]
보존: [인물, 얼굴, 액팅, 카메라, 배경, 조명 등]
금지: [재생성, 새로운 카메라, 새로운 액팅, 불필요한 추가 요소]
```

간단한 요청에서는 위 제목을 그대로 노출하지 않아도 된다.

필요한 내용을 빠뜨리지 않는 범위에서 하나의 자연스러운 프롬프트로 합친다.

기본 출력은 다음 형식을 따른다.

```text
한국어 검수본

[복사 가능한 완성형 한국어 Seedance 2.0 편집 프롬프트]


Seedance 입력용 영문 최적화본

[한국어 검수본과 요구사항이 완전히 동일한 완성형 영어 프롬프트]
```

사용자가 `한글만`이라고 지정하면:

```text
[완성형 한국어 프롬프트]
```

만 출력한다.

사용자가 `영어만`이라고 지정하면:

```text
[완성형 영어 프롬프트]
```

만 출력한다.

사용자가 `짧게`라고 하면 편집 대상, 변화, 핵심 보존 항목만 남겨 최대한 압축한다.

---

# 최우선 원칙

Seedance 2.0 영상 편집의 가장 중요한 목표는 다음과 같다.

**새로운 영상을 만드는 것이 아니라 원본 영상에서 사용자가 지정한 부분만 수정하는 것.**

항상 다음 순서를 우선한다.

1. 원본 영상 보존
2. 변경 대상 명확화
3. 인물 정체성 및 얼굴 보존
4. 기존 액팅 보존
5. 기존 카메라 움직임과 프레이밍 보존
6. 지정 영역만 국소 편집
7. 원근, 깊이, 조명과 가림 관계 유지
8. 물리적으로 자연스러운 FX
9. 불필요한 재생성 방지
10. 간결하고 실행 가능한 최종 프롬프트

---

## 출처 및 변형 정보

이 문서는 Seedance 2.0 Prompt Optimizer 계열의 편집 작업 구분, 멀티모달 자산 참조 방식과 영상 편집 지침을 기반으로 **한국어 영상 편집 실사용 중심으로 재구성한 파생 가이드**다.

원문을 단순 번역한 문서가 아니라 다음 항목을 강화한 버전이다.

- 한국어 GPT 출력 규칙
- 한국어 검수본 + Seedance 영문 최적화본 병행
- 원본 영상 잠금
- 인물 정체성 및 얼굴 보존
- 액팅 및 카메라 Invariant Lock
- 특정 요소만 교체
- 이미지 레퍼런스 속성 분리
- 국소 FX/VFX
- 건물 교체 + 폭발
- Relighting
- 원본 베이스 플레이트 방식의 Precision Edit
- 복합 편집 안정성
